manifest:
  remotes:
    - name: zmkfirmware
      url-base: https://github.com/zmkfirmware
      # Additional modules containing boards/shields/custom code can be listed here as well.
      # See:
      # - https://zmk.dev/docs/features/modules
      # - https://docs.zephyrproject.org/3.5.0/develop/west/manifest.html#projects
  projects:
    - name: eyelash_corne
      url: https://github.com/a741725193/zmk-new_corne
      revision: main
    - name: zmk
      remote: zmkfirmware
      # Pinned to last commit before Zephyr 4.1.0 / HWMv2 migration (b66a8cc on
      # 2026-03-26). The eyelash_corne board (HWMv1) is incompatible with
      # newer ZMK until the vendor migrates.
      revision: e4fb39d4a6514fe90d44b1570f908bddc5df0ad2
      import: app/west.yml
  self:
    path: config
